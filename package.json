{
  "name": "x-rocket",
  "version": "0.2.0",
  "description": "A dice-driven incremental rocket launch game.",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x",
    "npm": ">=11"
  },
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "electron:dev": "cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .",
    "build": "tsc --noEmit && vite build",
    "desktop": "npm run build && electron .",
    "dist": "npm run build && electron-builder",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@types/react": "^19.2.15",
    "@types/react-dom": "^19.2.3",
    "@tailwindcss/vite": "^4.3.0",
    "cross-env": "^10.1.0",
    "electron": "^42.0.1",
    "electron-builder": "^26.8.1",
    "tailwindcss": "^4.3.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.11",
    "vitest": "^4.1.5"
  },
  "build": {
    "appId": "com.barro.xrocket",
    "productName": "X Rocket",
    "files": [
      "dist/**/*",
      "electron/**/*",
      "package.json"
    ],
    "directories": {
      "buildResources": "public",
      "output": "release"
    },
    "win": {
      "target": "nsis"
    },
    "linux": {
      "target": "AppImage"
    },
    "mac": {
      "target": "dmg"
    }
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  }
}
